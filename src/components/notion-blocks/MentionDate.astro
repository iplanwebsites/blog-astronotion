---
import { Icon } from 'astro-iconify';
import {getTextToAstroIcon} from "../../lib/style-helpers.ts";

export interface Props {
	mention_date: string;
}

const { mention_date } = Astro.props;
const text = "🗓️";
const icon_name = getTextToAstroIcon(text);
---
{
  icon_name
    ? <Icon name={icon_name} class="inline h-5 w-5" aria-hidden="true"/>
    : text
} {' '+mention_date}
