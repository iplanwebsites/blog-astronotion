---
import NotionBlocks from '../NotionBlocks.astro'
import * as interfaces from '../../lib/interfaces'

export interface Props {
  block: interfaces.Block;
  headings: interfaces.Block[];
  renderChildren?: boolean; // Make this optional
  setId?:boolean;
}

const { block, headings, renderChildren = true, setId=true } = Astro.props;
---

<NotionBlocks blocks={block.SyncedBlock.Children} headings={headings} renderChildren={renderChildren} setId={setId} />
